毕生所学：数据诊断的目的：了解特征分布缺失异常情况，统计指标可以直接进行数据预处理
获取数据与读取：获取数据：kaggle 读取：pd
主要用的皮尔逊相关系数，欧式距离，Cosin相似度
皮尔逊相关系数：皮尔逊相关系数是用协方差除以两个变量的标准差得到的
协同过滤：1:相似度最好使用皮尔逊相似度
          2:考虑共同打分的物品数目，如min(n,N)*N,n为打分数，N为指定阈值
	  对打分进行归一化处理
	  设置一个相似度阈值
基于有两种方法：一个是基于用户的协同过滤，另一种是基于物品的协同过滤
                一般情况老说  大多数都是用基于物品的协同过滤 相比而言比较简便
相似：（相似度的打分平方+相似度打分平方）/（相似度+相似度）
       相似度是最接近相似的两个数
冷启动问题：
         根据榜单
	 文本分析
	 主题模型
	 打标签
	 推荐排行
	 新用户于新物品面临的问题
隐语义：
       某个物品在类所占的权重，也可以计算得出
       隐特征数一般设为100
       学习率 alpha 不能设置太大
       正则化参数lambda
       正负样本比例ration
多种的评估标准：
       准确率
       召回率
       覆盖率
       多样性
MSE：为优化损失
CNN,RNN都可做文本卷积，特征提取
分情况运用
如推荐系统中  做电影标题的文本卷积就用CNN，当然也可以用RNN
只不过在这个项目中就CNN比较快捷，效率高













ma
